//const libCluster = require '/lib/xp/cluster';
//log.info(`Starting ${app.name} ${app.version} isMaster:${libCluster.isMaster()} config:${JSON.stringify(app.config, null, 4)}`);
log.info(`Starting ${app.name} ${app.version} config:${JSON.stringify(app.config, null, 4)}`);
//__.disposer(function() {
	//log.info(`Stopping ${app.name} ${app.version} isMaster:${libCluster.isMaster()} config:${JSON.stringify(app.config, null, 4)}`);
	//log.info(`Stopping ${app.name} ${app.version} config:${JSON.stringify(app.config, null, 4)}`);
//});
